# 安全策略

## 报告安全问题

请通过仓库维护者的私密联系方式报告以下问题，不要创建公开 Issue：

- API key、访问令牌、私钥或数据库凭据泄漏；
- 学号、手机号、聊天记录等个人信息暴露；
- 身份认证、越权访问或会话隔离缺陷；
- Prompt injection 导致系统指令、私密知识或工具权限泄漏；
- 文档上传、解析或外部链接造成的代码执行和供应链风险。

报告中请包含受影响版本、复现步骤、实际影响和建议修复方式，但不要附带无关的真实个人数据。

## 仓库安全边界

- `.env.local` 和生产密钥不得进入 Git；
- 原始 QQ 记录、未审核学校资料、索引和运行日志不得进入 Git；
- 测试数据必须脱敏，且不得使用真实凭据；
- 知识内容被视为不可信输入，不能覆盖系统指令；
- 管理端摄取权限与普通问答权限必须隔离；
- 涉及时间、费用、入口和办理材料的回答必须优先引用有效官方资料。

如果密钥已经提交或出现在日志中，应立即在供应商控制台撤销并轮换；仅从 Git 历史删除并不能使密钥重新安全。
